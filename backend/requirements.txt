# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9

# Config / models
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# Supabase (Postgres + pgvector)
supabase>=2.6,<3.0
# Direct Postgres driver — only used for optional one-shot DDL migration
# (backend/migrate.py) when DATABASE_URL is set.
psycopg[binary]>=3.1,<4.0

# Embeddings — fastembed (ONNX, no torch; light enough for Render free 512MB)
fastembed>=0.4,<1.0

# Groq LLM via OpenAI-compatible client
groq>=0.11,<1.0

# Document text extraction + token-aware chunking
pypdf>=4.2
python-docx>=1.1
tiktoken>=0.7
